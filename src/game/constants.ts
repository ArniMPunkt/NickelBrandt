/**
 * Shared gameplay timing constants.
 */

/**
 * Duration of the "Hitster!" steal window (ms). Applies to BOTH modes (Hot-Seat
 * and Online) and is deliberately kept in ONE place so the two stay in sync —
 * change it here, never per-screen.
 */
export const STEAL_WINDOW_MS = 8000;
