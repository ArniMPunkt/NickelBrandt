# Local Android release signing secrets.
# Copy this file to `.env.signing.local` and fill in the real values.
# `.env.signing.local` is gitignored and must not be committed.

NICKELBRANDT_KEYSTORE_PASSWORD=__FILL OUT__
NICKELBRANDT_KEY_ALIAS=nickelbrandt
NICKELBRANDT_KEY_PASSWORD=__FILL OUT__
